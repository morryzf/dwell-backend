# 仓库里统一存 LF。
#
# 之前没有这个文件，行尾取决于每个人的 core.autocrlf——仓库里混进了 8 个
# CRLF / 混用行尾的文件。后果是任何工具重写其中一个，git 就会把整个文件的每
# 一行都算成改动，真正的一两行改动被埋在几百行假 diff 里。
#
# text=auto 让 git 自己判断哪些是文本，eol=lf 固定签出时也用 LF。

* text=auto eol=lf

# 二进制：别碰。
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.webp binary
*.woff  binary
*.woff2 binary
*.ttf  binary
*.otf  binary
*.mp3  binary
*.mp4  binary
*.db   binary
